/**
 * AUTO-GENERATED — do not edit by hand.
 *
 * Regenerate from the xlsx source(s) in `archive/raw-data/` by running:
 *   npm run import:form-data
 *
 * The form's Major (specialization) searchable combobox reads from this list.
 * Picking "Other" reveals a free-text input.
 */
export type Specialization = {
  /** Display name (program / major). */
  name: string;
  /** Optional source university — useful for filtering later if needed. */
  university?: string;
};

export const specializations: Specialization[] = [];
